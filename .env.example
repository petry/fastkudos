# Banco Neon (ou Postgres local para testes/dev)
DATABASE_URL=postgres://user:pass@host/db

# Segredo para assinar JWTs (HS256). Gere com: openssl rand -hex 32
JWT_SECRET=change-me-in-production

# URL pública da API (lida pelo front em build time)
VITE_API_URL=http://localhost:8787
